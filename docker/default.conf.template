server {
        listen 80 default_server;
        listen [::]:80 default_server;

        root /usr/share/nginx/html;

        server_name $SERVER_NAME;

        location / {
            try_files $uri $uri/ =404;
        }
}
